# RatioSense 앱 설계에 대한 전문가 의견서

**작성자:** 한인숙 (수학교육학 교수)
**작성일:** 2026년 3월 16일
**대상:** RatioSense - 초등학교 6학년 비와 비율 개념 학습용 다중감각 가상 조작물 앱

---

## 1. 각 논문의 핵심 설계 원리와 현재 RatioSense 앱의 반영 분석

### 1-1. Rick et al.(2012)의 "구체적 표상과 상징의 동시 연결"

Rick 등은 비례 추론 학습에서 구체적 표상(concrete representation)과 기호적 표상(symbolic notation)이 **동시에 화면에 제시되고 상호 연동**될 때 학습 효과가 극대화된다고 보았다. 현재 RatioSense 앱은 이 원리를 상당 부분 반영하고 있다. 색상 실험실에서 스포이드(구체적 조작) -> 비커 내 색상 변화(시각적 표상) -> 비율 표기(기호적 표상)가 동시에 제시되며, 화음 감상실에서도 피아노 건반(구체적) -> 파형 시각화(도상적) -> 주파수 비율(기호적)이 연동된다. 특히 `drawWaveAnim()` 함수에서 두 파형의 "만남점"을 실시간 표시하면서 동시에 `2:3` 같은 비율 기호를 보여주는 설계는 Rick 등의 원리를 잘 구현한 것이다.

그러나 **부족한 점**이 있다. 축소모형 탭에서 실물-모형 크기 비교 시각화(`compareVis`)가 있지만, 비례식의 외항-내항 관계를 시각적 화살표로만 표시할 뿐 학생이 직접 두 표상을 **드래그하여 연결하는** 상호작용이 없다. Rick 등이 강조한 것은 단순한 병렬 제시가 아니라 학생 자신이 **두 표상 사이를 능동적으로 오가며 연결을 만드는** 행위이다. 현재 도전 모드에서 비례식 입력(`propAnsW`, `propAnsH`)은 숫자 입력에 그치므로, 예컨대 실물 사각형의 변을 드래그하면 모형 사각형이 동시에 비례적으로 변하는 양방향 연동(bidirectional linking) 기능을 추가할 것을 제안한다.

### 1-2. Alberto et al.(2022)의 "역방향 발판(reverse-scaffolding)"과 학습 순서

Alberto 등의 행위 기반 체화 설계(action-based embodied design) 핵심은 세 단계 학습 순서이다: (1) **질적 단계**(qualitative stage) - 수치 없이 신체적 감각운동 조정 패턴을 발견, (2) **반성 단계**(reflecting step) - 자신의 행위를 언어로 기술, (3) **양적 단계**(quantitative stage) - 격자, 숫자 등 측정 도구 도입. 핵심은 기호를 **나중에** 도입하는 "역방향 발판"이다.

현재 RatioSense는 이 순서를 **부분적으로만** 따르고 있다. 색상 실험실은 비교적 잘 지키는 편이다. 학생이 먼저 스포이드로 물감을 넣어 색의 변화를 감각적으로 경험한 뒤, 저장 시점에서 `normRatio()` 함수가 비율을 기호적으로 보여준다. 그러나 화음 탐험실과 화음 감상실은 블록 수(`snd.a`, `snd.b`)와 주파수 비율(`ratio:[3,2]`)이 **처음부터** 숫자로 제시되어, 학생이 소리를 먼저 듣고 감각적으로 "어울림"을 느끼기 전에 이미 수치를 보게 된다. Alberto 등의 원리에 따르면, 처음에는 숫자 없이 슬라이더만으로 두 음의 높이를 조절하게 하고, 학생이 "어울린다"고 느끼는 지점을 먼저 찾은 후에 비로소 블록 수와 비율이 드러나는 설계가 바람직하다.

축소모형 탭의 경우, 탐험 모드에서 비율 칩(`1:5`, `1:10` 등)을 **먼저** 선택하고 결과를 보는 구조인데, 이보다는 학생이 모형 크기를 자유롭게 드래그하여 조절하다가 전시대에 "딱 맞는" 순간을 체화적으로 발견하고, 그 후에 "지금 비율이 1:20이에요!"라고 수치를 제시하는 순서가 Alberto 등의 원리에 더 부합한다.

### 1-3. Bush(2021)의 "세 층위 피드백"

Bush는 WMAL 소프트웨어에서 세 가지 층위의 피드백을 구분하였다: (1) **과제 피드백**(task feedback) - 정답/오답 판정, (2) **과정 지향 피드백**(process-oriented feedback) - 학생의 응답 패턴 분석에 기반한 전략 안내 영상, (3) **동적 조작물 피드백**(dynamic manipulative feedback) - 분수 막대, 원 등의 자연스러운 시각적 결과.

현재 RatioSense 앱의 피드백 구조를 세 층위로 분석하면:

- **과제 피드백(1층위):** 잘 구현되어 있다. `fb()` 함수로 정답 시 초록 `success`, 오답 시 노랑 `hint` 메시지를 표시하고, `boom()` 함수로 성공 애니메이션을 제공한다. 화채 만들기의 `checkRecipe()`에서 각 재료별 과부족을 구체적으로 알려주는 피드백도 우수하다.

- **동적 조작물 피드백(3층위):** 매우 우수하다. 비커 색상 변화(`updColor()`), 파형 만남점 시각화(`drawWaveAnim()`), 실물-모형 크기 비교(`compareVis`), 화채 그릇 채워짐 애니메이션(`bowlWater`) 등이 조작의 자연스러운 결과로서 즉시 시각적 피드백을 제공한다.

- **과정 지향 피드백(2층위):** 가장 보완이 필요한 부분이다. 도전 모드의 `showProcessHint()` 함수가 비례식 풀이 과정을 단계별로 안내하는 것은 좋으나, Bush가 강조한 **학생의 응답 패턴 분석에 기반한 적응적 피드백**은 부재하다. 예를 들어, 학생이 축소모형에서 반복적으로 덧셈적 사고(additive thinking)를 보이면("800에서 780을 빼면 되지 않나?"), 이를 감지하여 곱셈적 사고로 전환을 유도하는 맞춤형 영상이나 안내가 제공되어야 한다. 현재는 `propAttempts` 카운터로 3회 틀리면 정답을 보여주는 수준에 그치고 있다.

### 1-4. Moyer-Packenham & Westenskow(2013)의 "다섯 가지 어포던스"

Moyer-Packenham과 Westenskow는 66개 연구의 메타분석과 개념 분석을 통해 가상 조작물의 다섯 가지 어포던스를 도출하였다: (1) 초점화된 제약(focused constraint), (2) 창의적 변화(creative variation), (3) 동시적 연결(simultaneous linking), (4) 효율적 정밀성(efficient precision), (5) 동기 부여(motivation).

현재 RatioSense 앱을 다섯 어포던스별로 평가하면:

**(1) 초점화된 제약:** 화채 만들기에서 기본 레시피(1명분)를 고정하고 목표 인원수만 변경하도록 한 설계는 학생의 주의를 "배수 관계"에 집중시키는 우수한 제약이다. 나누기 모드의 단위 선택(`setDivUnit`)도 10ml/25ml/50ml로 제한하여 비례배분에 집중하게 한다. 다만, 색상 실험실은 삼원색을 자유롭게 조작할 수 있어 제약이 약하므로, 초기에는 두 색만 사용하도록 제한하는 단계를 추가하면 비의 이항 관계에 더 집중할 수 있다.

**(2) 창의적 변화:** 가장 잘 구현된 어포던스이다. 같은 비율의 다른 색을 만들어 보는 `recipe-match-group` 기능, 레시피 지도(`recipeMapCanvas`)에서 같은 비율의 점들이 원점을 지나는 직선 위에 놓이는 시각화는 학생 스스로 비의 동치 관계를 탐구하도록 돕는 창의적 설계이다.

**(3) 동시적 연결:** 앞서 Rick 등의 분석에서 언급한 바와 같이, 구체적-도상적-기호적 표상의 동시 제시는 상당 수준 구현되어 있으나, 양방향 연동은 부족하다.

**(4) 효율적 정밀성:** 디지털 특성을 활용하여 정확한 색상 혼합(`ryb2rgb()`), 정확한 주파수 비율(`pianoFreq()`), 정밀한 축소 계산(`W/modelScale`)을 제공한다. 물리적 조작물로는 불가능한 수준의 정밀성이다.

**(5) 동기 부여:** 별점 시스템(`rc-score-star`), 미션 단계별 진행, 성공 애니메이션(`boom()`), 다중감각(시각+청각) 활동은 동기 부여에 기여한다. 그러나 Moyer-Packenham이 강조한 **학생 주도의 탐구적 활동**(예: 자신만의 문제 만들기)은 부족하다.

---

## 2. 보완이 필요한 부분에 대한 구체적 제안

### 2-1. 곱셈적 관계 이해 촉진

비와 비율 개념의 핵심은 두 양 사이의 **곱셈적 관계**(multiplicative relationship)를 이해하는 것이다. 현재 앱에서 화채 만들기 미션 힌트("1명분 -> 3명분, 재료를 3배로!")는 곱셈적 사고를 명시적으로 안내하지만, 학생이 **스스로** 이를 발견하도록 유도하는 장치가 부족하다. 다음을 제안한다:

- **내적 비(within ratio)와 외적 비(between ratio)를 모두 경험하는 활동 추가:** 현재 화채 미션은 "1명분의 몇 배"(scalar/between ratio)만 다루고 있다. "수박 2조각에 딸기 3개"라는 재료 간 관계(functional/within ratio)도 탐구하도록, 재료 비율 시각화(예: 수박:딸기 막대 그래프의 높이 비)를 추가해야 한다.

- **덧셈적 오류를 진단하는 피드백:** 축소모형 도전 모드에서 학생이 "800-780=20"처럼 뺄셈으로 답하면, "비율은 빼기가 아니라 나누기로 구해요"라는 과정 피드백을 제시해야 한다.

### 2-2. 절차적 지식과 개념적 지식의 균형

현재 도전 모드의 비례식 풀이(외항의 곱=내항의 곱)는 절차적 지식에 치우쳐 있다. 탐험 모드에서의 자유 조작은 개념적 이해에 기여하지만, 두 모드 간 **명시적 연결**이 약하다. 탐험 모드에서 "왜 1:20이 맞는지" 발견한 직관을 도전 모드의 비례식 풀이와 연결하는 **중간 단계**(예: "아까 전시대에 딱 맞았던 비율을 기억하나요? 그것을 식으로 쓰면...")를 삽입할 것을 제안한다.

### 2-3. 교실 수업 활용 시나리오

현재 교사용 컨트롤(`teacher-panel`)은 리셋 기능만 있다. 실제 교실 활용을 위해서는:

- **전체 학급 공유 기능:** 학생들이 만든 색 레시피를 교사 화면에 실시간 모아 보여주는 기능. 같은 비율의 레시피가 여러 개 모이면 "비의 동치" 개념을 교실 전체에서 논의할 수 있다.
- **탐구 질문 생성기:** `showReflection()` 함수가 현재 무작위 40% 확률로 질문을 표시하는데, 교사가 특정 시점에 의도적으로 "지금 이 비율로 소리를 만들면?"이라는 교차영역 질문을 전체 학급에 띄울 수 있어야 한다.
- **학습 진행 데이터 수집:** 각 활동별 조작 로그(시도 횟수, 오류 유형, 소요 시간)를 교사 대시보드에 제공하여 형성평가에 활용.

---

## 3. 연구 논문으로서의 기여도를 높이기 위한 제안

### 3-1. 선행연구와의 차별점

RatioSense의 가장 독창적인 설계 요소는 **다중감각 교차 영역 연결(cross-domain multimodal linking)**이다. 기존 연구들(Rick의 tabletop, Bush의 WMAL, Alberto의 MIT)은 모두 단일 맥락(분수, 비례, 각도 등)에서 조작물을 설계하였으나, RatioSense는 색상-소리-축소모형-요리라는 네 가지 맥락에서 동일한 비율 구조를 경험하게 한다. `reflectionQuestions` 시스템에서 "이 색의 비율로 소리를 만들면?"과 같은 교차 질문을 제시하는 것은 Gentner(1983)의 구조 매핑 이론(structure mapping theory)에 기반한 **유추적 전이(analogical transfer)** 촉진 설계로서 학술적 차별점이 명확하다. 이 점을 논문에서 이론적 틀로 명시적으로 전면에 내세울 것을 권고한다.

또한 Alberto 등의 체화 설계가 주로 연구실 환경의 1:1 튜터링에서 검증된 반면, RatioSense는 **교실 환경에서 개별 학생이 독립적으로 사용**하는 웹 기반 도구이므로, 체화 설계 원리의 교실 확장(classroom scaling)이라는 연구 기여가 가능하다.

### 3-2. 효과 검증을 위한 연구 설계 방향

Bush(2021)의 무작위 교차 설계(randomized crossover design)를 참고하되, RatioSense의 특성에 맞게 다음을 제안한다:

- **설계:** 사전-사후 검사를 포함한 준실험 설계. 실험집단(RatioSense 활용)과 통제집단(교과서 기반 수업)의 비교.
- **측정 도구:** (a) 비와 비율 개념 이해 검사(곱셈적 사고 vs 덧셈적 사고 진단 문항 포함), (b) 표상 유연성 검사(동일 비율을 색상/소리/크기/양으로 전환하는 과제), (c) 유추적 전이 검사(학습하지 않은 새로운 맥락에서의 비율 적용).
- **분석:** Bush의 효과 크기 d=0.39를 참조 기준으로 삼되, 특히 (b)와 (c) 측정에서 RatioSense의 다중감각 설계가 차별적 효과를 보이는지 분석.
- **질적 분석:** Alberto 등의 방법론을 참고하여, 학생의 조작 과정 화면 녹화 및 발화 분석을 통해 교차영역 유추 전이가 실제로 발생하는지 미시적으로 검증.

---

## 종합 평가

RatioSense는 비와 비율이라는 추상적 수학 개념을 다중감각적 맥락에서 체험하게 하는 독창적인 가상 조작물이다. 네 편의 선행연구가 제시한 설계 원리 중 **동시적 표상 연결**(Rick), **동적 조작물 피드백**(Bush), **창의적 변화와 효율적 정밀성**(Moyer-Packenham)은 잘 반영되어 있다. 다만, Alberto 등의 **"신체적 조작 먼저, 기호 나중"** 원칙의 일관된 적용, Bush의 **과정 지향적 적응 피드백** 구현, 그리고 **곱셈적 관계의 명시적 진단과 촉진** 장치는 보완이 필요하다.

가장 큰 학술적 기여 가능성은 **다중 맥락 교차 유추**를 통한 비율 개념의 구조적 이해 촉진이라는 점에서, 이를 Gentner의 구조 매핑 이론과 연결하여 이론적 프레임워크를 정교화할 것을 권고한다.

한인숙 드림
